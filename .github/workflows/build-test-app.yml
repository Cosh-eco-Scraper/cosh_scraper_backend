name: build and test app
run-name: ${{ github.actor }} is running a build and test app
on: [ push ]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run installation"
      - run: npm install
      - run: npm run build
